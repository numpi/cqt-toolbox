%% Complex Conjugate Tranpose
%
% The syntax |B = A'|, which can also be called as |B = ctranspose(A)|,
% returns the complex conjugate transpose of the QT matrix. 

%% Syntax
% * |B = A'|
% * |B = ctranspose(A)|

%% Example
%

A = cqt([1 2], 1);
A'